package com.security.service.user;

import com.security.person.User;

public interface UserService {
	/**
	 * retrive a user by using userName and PassWord
	 * @param userName
	 * @return
	 */
	User getUserByUserName(String userName, String passWord);
}
